import { Milliseconds, Seconds } from '../shared';

export const DEFAULT_CLIENT_POLLING_INTERVAL: Milliseconds = 500;
export const DEFAULT_CLIENT_TIMEOUT: Milliseconds = 10000;
export const DEFAULT_CACHE_MAX_AGE: Seconds = 60;
export const DEFAULT_CACHE_TTL: Seconds = 50;
